import { SubmittableExtrinsic } from '@polkadot/api/types';
import { HexString } from '@polkadot/util/types';
import { ISubmittableResult } from '@polkadot/types/types';
import { IMessageSendOptions, IMessageSendReplyOptions, IMessageSendReplyWithVoucherOptions, IMessageSendWithVoucherOptions } from './types';
import { GearTransaction } from './Transaction';
import { ProgramMetadata } from './metadata';
import { UserMessageSentData } from './events';
export declare class GearMessage extends GearTransaction {
    /**
     * ## Send Message
     * @param args Message parameters
     * @param meta Program metadata obtained using `getProgramMetadata` function.
     * @param typeIndex (optional) Index of type in the registry. If not specified the type index from `meta.handle.input` will be used instead.
     * @returns Submittable result
     * @example
     * ```javascript
     * const programId = '0x..';
     * const hexMeta = '0x...';
     * const meta = getProgramMetadata(hexMeta);
     *
     * const tx = api.message.send({
     *   destination: programId,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000
     * }, meta, meta.handle.input)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    send(args: IMessageSendOptions, meta: ProgramMetadata, typeIndex?: number): SubmittableExtrinsic<'promise', ISubmittableResult>;
    /**
     * ## Send Message
     * @param args Message parameters
     * @param hexRegistry Registry in hex format
     * @param typeIndex Index of type in the registry.
     * @returns Submitted result
     * @example
     * ```javascript
     * const programId = '0x..';
     * const hexRegistry = '0x...';
     *
     * const tx = api.message.send({
     *   destination: programId,
     *   payload: { amazingPayload: { ... } },
     *   gasLimit: 20_000_000
     * }, hexRegistry, 4)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    send(args: IMessageSendOptions, hexRegistry: HexString, typeIndex: number): SubmittableExtrinsic<'promise', ISubmittableResult>;
    /**
     * ## Send Message
     * @param args Message parameters
     * @param metaOrHexRegistry (optional) Registry in hex format or ProgramMetadata
     * @param typeName payload type (one of the default rust types if metadata or registry don't specified)
     * @returns Submitted result
     * @example
     * ```javascript
     * const programId = '0x..';
     *
     * const tx = api.message.send({
     *   destination: programId,
     *   payload: 'PING',
     *   gasLimit: 20_000_000
     * }, undefined, 'String')
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    send(args: IMessageSendOptions, metaOrHexRegistry?: ProgramMetadata | HexString, typeName?: string): SubmittableExtrinsic<'promise', ISubmittableResult>;
    /**
     * ## Send reply message
     * @param args Message parameters
     * @param meta Program metadata obtained using `getProgramMetadata` function.
     * @param typeIndex (optional) Index of type in the registry. If not specified the type index from `meta.reply.input` will be used instead.
     * @returns Submitted result
     * @example
     * ```javascript
     * const replyToMessage = '0x..';
     * const hexMeta = '0x...';
     * const meta = getProgramMetadata(hexMeta);
     *
     * const tx = api.message.send({
     *   replyToId: replyToMessage,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000
     * }, meta, meta.reply.input)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendReply(args: IMessageSendReplyOptions, meta?: ProgramMetadata, typeIndex?: number): SubmittableExtrinsic<'promise', ISubmittableResult>;
    /**
     * ## Send reply message
     * @param args Message parameters
     * @param hexRegistry Registry in hex format
     * @param typeIndex Index of type in the registry.
     * @returns Submitted result
     * @example
     * ```javascript
     * const replyToMessage = '0x..';
     * const hexRegistry = '0x...';
     *
     * const tx = api.message.send({
     *   replyToId: replyToMessage,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000
     * }, hexRegistry, 5)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendReply(args: IMessageSendReplyOptions, hexRegistry: HexString, typeIndex: number): SubmittableExtrinsic<'promise', ISubmittableResult>;
    /**
     * ## Send reply message
     * @param args Message parameters
     * @param metaOrHexRegistry (optional) Registry in hex format or ProgramMetadata
     * @param typeName payload type (one of the default rust types if metadata or registry don't specified)
     * @returns Submitted result
     * ```javascript
     * const replyToMessage = '0x..';
     * const hexRegistry = '0x...';
     *
     * const tx = api.message.send({
     *   replyToId: replyToMessage,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000
     * }, hexRegistry, 5)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendReply(args: IMessageSendReplyOptions, metaOrHexRegistry?: ProgramMetadata | HexString, typeName?: string): SubmittableExtrinsic<'promise', ISubmittableResult>;
    /**
     * ## Send Message with Voucher
     * @param args Message parameters
     * @param meta Program metadata obtained using `getProgramMetadata` function.
     * @param typeIndex (optional) Index of type in the registry. If not specified the type index from `meta.handle.input` will be used instead.
     * @returns Submittable result
     * @example
     * ```javascript
     * const programId = '0x..';
     * const hexMeta = '0x...';
     * const meta = getProgramMetadata(hexMeta);
     * const accountId = '0x...'
     *
     * const tx = await api.message.sendWithVoucher({
     *   destination: programId,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000,
     *   account: accountId,
     * }, meta, meta.handle.input)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendWithVoucher(args: IMessageSendWithVoucherOptions, meta: ProgramMetadata, typeIndex?: number): Promise<SubmittableExtrinsic<'promise', ISubmittableResult>>;
    /**
     * ## Send Message with Voucher
     * @param args Message parameters
     * @param hexRegistry Registry in hex format
     * @param typeIndex Index of type in the registry.
     * @returns Submitted result
     * @example
     * ```javascript
     * const programId = '0x..';
     * const hexRegistry = '0x...';
     * const accountId = '0x...'
     *
     * const tx = await api.message.sendWithVoucher({
     *   destination: programId,
     *   payload: { amazingPayload: { ... } },
     *   gasLimit: 20_000_000
     *   account: accountId,
     * }, hexRegistry, 4)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendWithVoucher(args: IMessageSendWithVoucherOptions, hexRegistry: HexString, typeIndex: number): Promise<SubmittableExtrinsic<'promise', ISubmittableResult>>;
    /**
     * ## Send Message with Voucher
     * @param args Message parameters
     * @param metaOrHexRegistry (optional) Registry in hex format or ProgramMetadata
     * @param typeName payload type (one of the default rust types if metadata or registry don't specified)
     * @returns Submitted result
     * @example
     * ```javascript
     * const programId = '0x..';
     * const accountId = '0x...'
     *
     * const tx = await api.message.sendWithVoucher({
     *   destination: programId,
     *   payload: 'PING',
     *   gasLimit: 20_000_000,
     *   account: accountId,
     * }, undefined, 'String')
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendWithVoucher(args: IMessageSendWithVoucherOptions, metaOrHexRegistry?: ProgramMetadata | HexString, typeName?: string): Promise<SubmittableExtrinsic<'promise', ISubmittableResult>>;
    /**
     * ### Send reply message with voucher
     * @param args Message parameters
     * @param meta Program metadata obtained using `getProgramMetadata` function.
     * @param typeIndex (optional) Index of type in the registry. If not specified the type index from `meta.reply.input` will be used instead.
     * @returns Submitted result
     * @example
     * ```javascript
     * const replyToMessage = '0x..';
     * const hexMeta = '0x...';
     * const meta = getProgramMetadata(hexMeta);
     * const accountId = '0x...'
     *
     * const tx = await api.message.sendReplyWithVoucher({
     *   replyToId: replyToMessage,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000,
     *   account: accountId,
     * }, meta, meta.reply.input)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendReplyWithVoucher(args: IMessageSendReplyWithVoucherOptions, meta?: ProgramMetadata, typeIndex?: number): Promise<SubmittableExtrinsic<'promise', ISubmittableResult>>;
    /**
     * ### Send reply message with voucher
     * @param args Message parameters
     * @param hexRegistry Registry in hex format
     * @param typeIndex Index of type in the registry.
     * @returns Submitted result
     * @example
     * ```javascript
     * const replyToMessage = '0x..';
     * const hexRegistry = '0x...';
     * const accountId = '0x...'
     *
     * const tx = await api.message.sendReplyWithVoucher({
     *   replyToId: replyToMessage,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000,
     *   account: accountId,
     * }, hexRegistry, 5)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendReplyWithVoucher(args: IMessageSendReplyWithVoucherOptions, hexRegistry: HexString, typeIndex: number): Promise<SubmittableExtrinsic<'promise', ISubmittableResult>>;
    /**
     * ### Send reply message with voucher
     * @param args Message parameters
     * @param metaOrHexRegistry (optional) Registry in hex format or ProgramMetadata
     * @param typeName payload type (one of the default rust types if metadata or registry don't specified)
     * @returns Submitted result
     * @example
     * ```javascript
     * const replyToMessage = '0x..';
     * const hexRegistry = '0x...';
     * const accountId = '0x...'
     *
     * const tx = await api.message.sendReplyWithVoucher({
     *   replyToId: replyToMessage,
     *   payload: { amazingPayload: { } },
     *   gasLimit: 20_000_000,
     *   account: accountId,
     * }, hexRegistry, 5)
     *
     * tx.signAndSend(account, (events) => {
     *   events.forEach(({event}) => console.log(event.toHuman()))
     * })
     * ```
     */
    sendReplyWithVoucher(args: IMessageSendReplyWithVoucherOptions, metaOrHexRegistry?: ProgramMetadata | HexString, typeName?: string): Promise<SubmittableExtrinsic<'promise', ISubmittableResult>>;
    listenToReplies(programId: HexString, bufferSize?: number): (messageId: HexString) => Promise<UserMessageSentData>;
}
