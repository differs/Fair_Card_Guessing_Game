import { HexString } from '@polkadot/util/types';
import { HumanProgramMetadataRepr } from '../types';
import { GearMetadata } from './metadata';
export declare class ProgramMetadata extends GearMetadata {
    types: Omit<HumanProgramMetadataRepr, 'reg'>;
    constructor({ reg, ...types }: HumanProgramMetadataRepr);
}
export declare function getProgramMetadata(hexMetadata: HexString | string): ProgramMetadata;
