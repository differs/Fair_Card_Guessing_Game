import { ReactNode } from 'react';
import { TransitionProps } from 'react-transition-group/Transition';
type Props = Partial<TransitionProps> & {
    children: ReactNode;
};
declare const Transition: ({ children, ...props }: Props) => JSX.Element;
export { Transition };
